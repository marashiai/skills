interface:
  display_name: "Engineering"
  short_description: "Ship production-ready, runtime-verified changes"
  default_prompt: "Use $engineering to implement the smallest honest solution, then prove the release artifact in the closest safe production-equivalent runtime."
